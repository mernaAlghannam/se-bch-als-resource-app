export type ResourceLink = {
  id: string;
  title: string;
  url: string;
}

export type HandoutOrTestimonialLink = {
  id: string;
  title: string;
  url: string;
}

export type PageContentType = {
  paragraph: string;
  imageURL: string;
  videoURL: string;
}