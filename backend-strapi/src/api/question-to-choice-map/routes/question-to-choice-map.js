'use strict';

/**
 * question-to-choice-map router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::question-to-choice-map.question-to-choice-map');
