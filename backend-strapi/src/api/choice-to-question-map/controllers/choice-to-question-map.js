'use strict';

/**
 * choice-to-question-map controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::choice-to-question-map.choice-to-question-map');
