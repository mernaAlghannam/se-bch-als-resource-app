'use strict';

/**
 * choice-to-question-map service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::choice-to-question-map.choice-to-question-map');
