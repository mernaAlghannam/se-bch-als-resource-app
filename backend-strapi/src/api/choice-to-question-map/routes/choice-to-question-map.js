'use strict';

/**
 * choice-to-question-map router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::choice-to-question-map.choice-to-question-map');
